<!--index.wxml-->
<view class='map-container'>
 <map id="map" class="map" longitude="{{longitude}}" latitude="{{latitude}}" scale="14" bindcontroltap="controltap" controls="{{controls}}" show-location></map>
</view>

<view class='btns'>
    <view class='btn btn-left'> 
      <navigator url="/pages/publish/publish">发布</navigator>
    </view>
    <view class='btn'>搜索</view>

</view>
